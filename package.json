{
  "name": "session-cookie-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private universal cookie session export/import extension (MV3) with signed artifacts.",
  "engines": {
    "node": ">=18.20.8 <19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "icons:generate": "python3 scripts/generate-minimal-icons.py",
    "package:zip": "npm run build && node scripts/package-zip.mjs"
  },
  "overrides": {
    "cheerio": "1.0.0-rc.12"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.1",
    "@types/chrome": "^0.1.24",
    "@types/node": "^18.19.0",
    "archiver": "^7.0.1",
    "type-fest": "^5.4.4",
    "typescript": "^5.7.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  }
}
